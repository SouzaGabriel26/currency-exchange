export interface TradeInterface {
  id: string,
  userId: string,
  createdAt: string;
  currentBidValue: string;
  currentBidDate: string;
  description: string;
  inputValue: number;
  outputValue: number;
}
